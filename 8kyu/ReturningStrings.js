// Returning Strings

function greet(name){
    //   return a greeting statement that uses an input
      return `Hello, ${name} how are you doing today?`
}
    
// we can use a template literal string