// Convert number to reversed array of digits

const digitize= (n) => n.toString().split('').map(Number).reverse();

// get number
// number(s) into array
// number(s) into a string
// reverese elements in array

// .reverse takes a list and reverses the elements
// declare function digitize
// map each element and turn it back into a number