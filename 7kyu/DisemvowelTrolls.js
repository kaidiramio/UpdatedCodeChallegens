// Disemvowel Trolls (remove vowels from string)

function disemvowel(str) {
    return str.replace(/[aeiou]/gi, '')
   //  case insensitive serach lower and upper case = gi
// replace method
}
   



// Replace method https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/replace
// The replace() method returns a new string with some or all matches of a pattern replaced by a replacement. The pattern can be a string or a RegExp, and the replacement can be a string or a function called for each match.

// The gi modifier is used to do a case insensitive search of all occurrences of a regular expression in a string.

// Regular expressions are patterns used to match character combinations in strings.

// started w/ 
// let Nstr = ['a', 'e', 'i', 'o', 'u']
// return str.filter();